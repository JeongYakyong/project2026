# 8-0. Streamlit 데모 컨셉 (착수 전 구상)

> 작성 2026-06-10. 다음 세션에서 8단계 착수 시 이 문서 + 루트 `PROJECT.md`(§6)를 기준으로 시작한다.
> 아래 "결정 필요(G-15 후보)"를 먼저 확정한 뒤 구현에 들어간다(§0.6 작업 쪼개기 규율).

---

## 1. 컨셉 (한 줄)

**"신재생이 만든 잔여부하를 가스 발전이 메운다 — 그 체인(수요 → 신재생 → net_load → 가스)을 매일의 예측으로 보여주는 발전사업자용 브리핑 대시보드."**

- 정확도 자랑 페이지가 아니라 **명제 입증(§1.2)을 시각적으로 전달**하는 페이지다.
- 차별점 = ① 5→6→7 실제 서빙 체인이 살아서 돈다 ② 실측 대비 오차를 숨기지 않는다(정직성 §5.4) ③ brief_ai가 수치를 자연어 브리핑으로 바꿔준다(공모전 가점 의도).

## 2. 목표 (공모전 심사 관점)

1. **명제가 한 화면에서 읽힌다**: 신재생↑ → net_load↓ → 가스↓ 의 연결을 차트 스택으로 보여준다.
2. **살아있는 예측**: 기준일(origin)을 고르면 D+1~D+7(가스는 D+1~D+12)의 예측 곡선과, 지난 날짜는 실측 비교까지 나온다.
3. **KOGAS 연결(자격 앵커 §2)**: 가스 발전량(MWh) → 송출량(TON, ×0.1521) → 가스비(원) 환산을 화면에 명시한다.
4. **정직성**: 단계별 MAPE, 체인 손실(ORACLE 10.8% vs 체인 ~13%), 자기참조 구간 "추정" 표기를 검증 탭에 그대로 둔다.
5. brief_ai 브리핑(3~5문장)으로 "그래서 발전사업자는 뭘 하면 되는가"를 답한다.

## 3. 디자인

> **2026-06-10 개편 — 아래 3.1/3.2 구상은 v2(§3.4)로 대체됨.** 단일 탭 구조 대신 멀티 페이지 + 사이드바 메뉴로 확정. 3.3 brief_ai는 유지.

### 3.4 확정 디자인 v2 (2026-06-10, 8-A 구현 기준)

**멀티 페이지**: 전국 / 제주 (st.navigation). 각 페이지의 사이드바 radio 메뉴(공통 골격):

| 메뉴 | 내용 |
|---|---|
| **종합** | 탭 4개 — ① **예측 확인**(구 '현황'): 하루 단위 날짜 네비(◀ 전일/날짜/익일 ▶/오늘/실시간 새로고침, 기본=오늘) + 상단 선택일 예상 가스 송출량(+발전 합·가스비) + **공통 비교 plot**(`render_series_compare`, 장지평과 공용): 시리즈 9종 = 수요/가스/신재생/net_load 각 실측·예측 + **KPX 수요예측(DA, dash — 전력거래소 비교용)**, ⚙️ popover 선택(기본 6종 on·net_load/KPX off) + 하단 AI brief. 예측 없는 날짜는 제한 실행 버튼 ② **발전데이터**(2026-06-11 신설): 전력거래소식 **누적 발전 믹스**(실측 5그룹: 원전 기저→기타발전→가스→태양광+풍력→BTM+PPA 추정, **투명 색 rgba 0.45**) + 총수요 선(연한 회청 #455a64) + **예측 dot 3종 — 가스·태양광+풍력(아래 누적을 더한 같은 기준으로 정렬, 점선↔띠 윗변 간격=오차) + 전력수요(총수요 기준 = est_true_demand_land, 6단계)** + **미수집 시간 절단**(가스≤0/결측 시간은 면적 미표시 — 절벽 방지). 자체 날짜 네비 ③ **기상개황**(2026-06-11 인포그래픽 확정): **시도 choropleth 히트맵** — 5개 수집 지점의 예보를 IDW(1/d²)로 17개 시도에 보간(서울 등 미수집 지역까지 전 국토 커버) + **신재생 중요지역 원**(태양광 주황 2025·풍력 청록 2023, 비중∝크기·진하기, 같은 시도는 좌/우 오프셋) + 수집 5지점(파랑, hover 3변수)·참고 13지점(회색) 마커. 컨트롤=날짜(오늘/내일)·변수(기온/일사/풍속)·시각 slider + 하단 5지점 카드. 자산: `9. design/skorea_provinces_simplified.json`(7.2MB→0.24MB 단순화)·`weather_map.py` ④ **장지평 예측**(2026-06-11 개편): 시작일 선택(기본 오늘, 과거 가능) + **끝 날짜 슬라이더**(최대 14일 창, 적재 한계 동적). **이중 모드** — ⓐ 미래(시작일≥오늘) = 최신 발행 D+1~D+7(forecast 테이블, 고정 origin) ⓑ **과거 = 지평 모드**: 발행 시점 radio(D+1/2/3/7/12) → "k일 전 발행" 예측 vs 실측. 데이터 = 7-A2-A `chained_gas_dataset.parquet`(수요 5-A2·신재생 6단계 지평별, 2022-01~) + 가스는 7-A2 모델 즉석 계산(serve_land_gas 자산 importlib 재사용). 구간 합 송출량·가스비 + **가스 MAPE 지표**(지평 전환 시 평평함이 바로 보임 — 데모 핵심) + 공통 비교 plot 9종. ※ forecast 테이블 과거 값은 rolling D+1 백필이라 "과거 고정 origin 장지평"은 parquet 경로로만 가능(timestamp당 1값 구조 한계). 미래 D+12 확장은 5-A2 D+12 서빙 연결 후 |
| **수요 예측** | **공통 시간 컨트롤**(2026-06-11 개편: ◀ 전일/날짜[과거 가능]/익일 ▶/오늘/실시간 새로고침 + 표시 기간 슬라이더 1~7일 — 4개 탭이 같은 구간 공유) + 탭 4개 — 전력수요(실측·예측+**KPX 수요예측 DA dash**) / 순수요(net_load) / 천연가스(구간 송출량·가스비+MW·TON) / **검증**(체인 스택 4행[수요·신재생·net_load·가스, 실측 solid vs 예측 dot, x축 공유] + 구간 MAPE·MAE·bias 배지[신재생은 nMAE] + 최근 30일 오차 추이 + 예측 없는 구간은 제한 실행 버튼) |
| **데이터 현황** | DB 적재 현황·신선도 테이블(주요 컬럼 보유 기간·행수·경과시간) |
| **SMP 예측** | **제주 페이지만** (G-15 ⑤ 번복, 2026-06-10) |

- 선 규약: **실측 = solid, 예측 = dot** (전 화면 공통).
- 지평 slider는 사전 적재된 DB를 읽기만 하므로 지연 없음(최신 origin 기준 D+1~D+7 체인을 cron으로 적재).
- 파일: `app.py`(엔트리) + `common.py`(조회 레이어) + `page_land.py` + `page_jeju.py` — §4의 "단일 app.py, pages/ 없이"는 멀티 페이지로 변경.

### 3.1 사이드바
- **지역**: 전국 / 제주 (라디오)
- **기준일(origin)**: 날짜 선택 — 데이터 보유 기간으로 제한(§6.3)
- **지평**: D+1 ~ D+7 슬라이더 (가스는 D+12까지)
- (관리자) 캐시 초기화

### 3.2 탭 구조 (§6.1 골격 유지 + 보완 2건)

| 탭 | 내용 | 데이터 출처 |
|---|---|---|
| **Tab 1. 개요** | 명제 한 장 다이어그램(수요−신재생=net_load→가스), 체인 구조 5→6→7, 데이터 귀속(KOGAS 앵커) | 정적 |
| **Tab 2. 예측 대시보드 (핵심)** | 상단 brief_ai 카드 → 차트 스택: 기상 개황 / 수요 vs 신재생 / net_load / **가스 송출량(TON)+가스비** → 하단 시간대별 테이블. 지난 날짜는 실측 오버레이 | forecast 테이블: `est_demand_land`·`est_market_renew_land`·`est_net_load_land`·`est_gas_gen_land`·`est_gas_sendout_ton_land` (제주: `jeju_est_demand_lh`·`est_*_jeju_lh`·`est_net_load_jeju`·SMP) |
| **Tab 3. 모델 검증·정직성** | 단계별 MAPE 표(5: ~3.5% / 6: util MAE / 7: 체인 ~13% vs ORACLE 10.8%), 최근 오차 추이, **제주 7-B 대체효과(PDP −0.314) vs 전국(−0.017) 대비표** = 명제 입증 핵심 그림 | historical + 각 단계 리포트 수치 |
| **Tab 4. 전국 확장·KOGAS** | 검증목표 2 결과(`gen_gas_kr` 실측 검증), 변환계수 0.1521 ton/MWh(열효율 ~43%), 월 가스비 규모, 시장 구조 설명 박스(전국 SMP 비대상 사유) | 7-C 산출물 |

**보완 ①(제주 분기)**: 제주는 가스 서빙이 없다(7-B는 명제 probe). 제주 선택 시 Tab 2의 마지막 카드는 가스 대신 **SMP D+1/D+2 + 음수가격 경보**(4단계, ROC-AUC 0.973)를 보여준다. → 제주=시장(SMP) 관점, 전국=가스 관점으로 지역별 결말이 자연스럽게 갈린다.

**보완 ②(SMP 명시)**: 기존 §6.1엔 4단계 SMP가 빠져 있었다. 위처럼 제주 Tab 2에 편입하는 것으로 반영한다.

### 3.3 brief_ai
- 입력: 당일 예측 요약(기상·수요·신재생·net_load·가스 또는 SMP, 전일 실측 대비)을 작은 JSON으로.
- 출력: 3~5문장 — 기상 개황 / net_load 변동 / 가스(또는 SMP) 동향 / 발전사업자 액션 제안.
- 같은 (날짜, 지역)은 24시간 캐시. API 실패 시 "AI 브리핑 일시 사용 불가" + 차트는 그대로(§6.2).

## 4. 기술 골격 (가볍게)

- 단일 `app.py` + `pages/` 없이 탭으로(데모 규모에 중첩 불필요). 차트는 plotly.
- DB는 **읽기 전용**: `1. data_fetcher_and_db/data/input_data_{jeju,land}.db` 의 forecast·historical만 SELECT. 데모 앱에서 서빙 스크립트를 직접 실행하지 않는다(예측은 사전 적재된 값을 보여주는 방식 권장 — 아래 G-15 ③).
- `st.cache_data(ttl=...)`로 쿼리 캐시. 예측 실행 버튼을 둔다면 IP당 횟수 제한(§6.3).

## 5. 결정 필요 — G-15 후보 (구현 전 확정, §0.3)

> **2026-06-10 확정(PROJECT.md §7 G-15)**: ① 자체 서버 호스팅(로컬 DB 실시간 읽기) ② Gemini API 유지 ③ 사전 적재 기본 + 시연용 실행 버튼 병행 ④ 표시 기간 = 데이터 보유 범위(전국 2026-02~, 제주 2025-12~) ⑤ SMP 데모에서 일단 제외(제주는 net_load까지).

1. **배포 위치 + DB 접근**: Streamlit Community Cloud는 로컬 sqlite 파일 동기화가 안 됨. 후보 = (a) 주기적 DB 스냅샷을 repo/스토리지에 푸시 (b) 자체 서버(집/클라우드 VM)에서 streamlit 직접 호스팅 (c) 데모용 정적 추출(parquet) 동봉. **앱 코드보다 이 결정이 먼저다.**
2. **brief_ai LLM**: 기존 명세는 Gemini API(§6.2). 유지/변경 + API 키·비용 확인.
3. **예측 갱신 방식**: 실시간 실행 버튼 vs **사전 적재(서빙 3종을 cron으로 5→6→7 순서 실행, 앱은 읽기만)**. 사전 적재 권장 — PROJECT.md 지적사항(체인 자동화 공백)도 같이 해소된다.
4. **표시 기간**: 백필을 언제부터 보여줄지(자기참조 구간 "추정" 표기 포함).
5. **SMP 포함 여부**: 위 보완 ① 제안대로 제주 탭에 편입할지 최종 확정.

## 6. 작업 쪼개기 (제안)

- **8-0** 이 문서 + G-15 확정 ← 지금
- **8-A** DB 조회 레이어 + Tab 2 전국(체인 차트) — 뼈대 완성이 최우선
- **8-B** Tab 2 제주(SMP 편입) + Tab 1 개요
- **8-C** Tab 3 검증 + Tab 4 KOGAS
- **8-D** brief_ai 연결 + 캐시·제한
- **8-E** 배포 + 시연 영상 누적 시작(§9 체크리스트 — 마감 7/03 역산상 8-E가 빠를수록 좋다)
